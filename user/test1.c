#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int main(){
    int pid0 = fork();
    int pid1 = fork();
    int pid2 = fork();
    if(pid0 == 0){

    }
    else if(pid1 == 0){

    }
    else if (pid2 == 0){

    }
    else{
        
    }
    for(int i=0;;i++){

    }

    exit(0);
}